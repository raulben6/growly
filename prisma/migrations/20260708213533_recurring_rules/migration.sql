-- This migration creates the RecurringRule model and adds recurringRuleId to Transaction.
-- The enum, tables, and columns have already been created by prisma db push,
-- so this migration only records the change in the migrations table.
-- The database is already in sync with the schema.
SELECT 1;
